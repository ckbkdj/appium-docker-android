[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"

[project]
name = "semantic-mobile-agent"
version = "0.1.0"
description = "Token-efficient semantic Android control agent with Bridge/ADB/Appium execution."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "ckbkdj" }]
dependencies = [
  "fastapi>=0.115,<1",
  "uvicorn[standard]>=0.30,<1",
  "pydantic>=2.9,<3",
  "pydantic-settings>=2.5,<3",
  "httpx>=0.27,<1",
  "aiosqlite>=0.20,<1",
  "PyYAML>=6.0.2,<7",
]

[project.optional-dependencies]
appium = ["Appium-Python-Client>=4.0,<6"]
mcp = ["mcp>=1.8,<3"]
dev = [
  "build>=1.2,<2",
  "pytest>=8.3,<10",
  "pytest-asyncio>=0.24,<2",
  "ruff>=0.9,<1",
  "mypy>=1.14,<3",
]
all = [
  "Appium-Python-Client>=4.0,<6",
  "mcp>=1.8,<3",
]

[project.scripts]
mobile-agent-api = "mobile_agent.__main__:main"
mobile-agent-mcp = "mobile_agent.mcp_server:main"
mobile-agent-bench = "mobile_agent.benchmark:main"

[tool.hatch.build.targets.wheel]
packages = ["src/mobile_agent"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "ASYNC"]
ignore = ["B008", "E501"]

[tool.mypy]
python_version = "3.11"
strict = true
warn_unused_ignores = false
plugins = ["pydantic.mypy"]
