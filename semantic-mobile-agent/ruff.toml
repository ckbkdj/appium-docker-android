line-length = 100
target-version = "py311"

[lint]
select = ["E", "F", "B"]
ignore = ["E501"]

[lint.per-file-ignores]
# `Any` remains imported in the engine module as part of its public extension
# typing surface; runtime correctness is covered by compileall and tests.
"src/semantic_mobile_agent/engine.py" = ["F401"]
